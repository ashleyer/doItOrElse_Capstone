import React from 'react';
import { Link } from 'react-router-dom';
import '../App.css';

const TaskCard = (props) => {
    const task = props.TaskCard;

    return(
        <div>
            <h2>hello from taskcard</h2>
        </div>
    )
}
export default TaskCard;